apiVersion: apps/v1
kind: Deployment
metadata:
  name: camserver
  namespace: efficient-edge-demo
  labels:
    app: camserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: camserver
  template:
    metadata:
      labels:
        app: camserver
    spec:
      containers:
      - name: camserver 
        command: ["sleep"]
        args: ["infinity"]
        image: quay.io/oglok/yolo-server:agx
        imagePullPolicy: Always
        ports:
         - containerPort: 5000

